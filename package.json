{
  "name": "escapist",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "concurrently -n client,server,worker -c blue,green,yellow \"npm run dev:client\" \"npm run dev:server\" \"npm run dev:worker\"",
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && npm run dev",
    "dev:worker": "cd cli-worker && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
