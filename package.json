{
  "name": "escapist",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@10.12.4",
  "scripts": {
    "dev": "concurrently -n client,server,worker -c blue,green,yellow \"pnpm --filter @escapist/client dev\" \"pnpm --filter @escapist/server dev\" \"pnpm --filter @escapist/cli-worker dev\"",
    "build": "pnpm --filter @escapist/client build && pnpm --filter @escapist/server build",
    "typecheck": "pnpm -r --parallel typecheck"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["better-sqlite3", "esbuild"]
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
